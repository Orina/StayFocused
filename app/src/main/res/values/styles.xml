<resources>

    <!-- Base application theme. -->

    <style name="StayFocusedTheme" parent="StayFocusedTheme.Base">
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="StayFocusedTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="StayFocusedTheme.TextAppearance" >
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="StayFocusedTheme.TextAppearance.TaskItem" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">@dimen/task_item_text_size</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>

    </style>

    <style name="StayFocusedTheme.TextAppearance.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="StayFocusedTheme.TextAppearance.Title.Inverse" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!--Floating label text style-->
    <style name="StayFocusedTheme.TextAppearance.HintText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorAccentLight</item>
    </style>

    <!--Input field style-->
    <style name="StayFocusedTheme.TextAppearance.EditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimaryVeryLight</item>
        <item name="colorControlActivated">@color/colorAccentLight</item>
        <item name="colorControlHighlight">@color/colorAccentLight</item>
    </style>

    <style name="StayFocusedTheme.DialogPicker" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="textColorAlertDialogListItem">@android:color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:windowBackground">@drawable/background_date_picker_dialog</item>
    </style>

    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- THIS is where you can color the arrow! -->
        <item name="colorControlNormal">@android:color/white</item>
    </style>

</resources>
